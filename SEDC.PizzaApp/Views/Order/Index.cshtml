

@using SEDC.PizzaApp.Models.ViewModels
@model List<OrderDetailsViewModel>

<ol>
    @for (int i = 0; i < Model.Count; i++)
    {
        <li>@Model.ElementAt(i).PizzaName - @Model.ElementAt(i).UserFullName - @Model.ElementAt(i).Price denari.</li>
    }
</ol>